<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/iv_doctor_top"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:src="@drawable/doctor_img"
        />

    <TextView
        android:id="@+id/tv_doctorName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@+id/iv_doctor_top"
        app:layout_constraintBottom_toBottomOf="@+id/iv_doctor_top"
        app:layout_constraintStart_toEndOf="@+id/iv_doctor_top"
        android:text="Doctor Name"
        android:textColor="@color/black"
        android:textSize="18dp"
        android:layout_marginLeft="@dimen/margin_large"/>

</androidx.constraintlayout.widget.ConstraintLayout>